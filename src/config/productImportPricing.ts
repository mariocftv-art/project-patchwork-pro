/**
 * Referências aprovadas para futuras importações de produtos.
 * Estes valores não são aplicados automaticamente e exigem autorização antes do uso.
 */
export const PRODUCT_IMPORT_PRICE_REFERENCES = [
  {
    minimumCost: 60,
    maximumCost: 90,
    referenceSalePrice: 330,
  },
] as const;
